# TODO

Критерий готовности первой версии: не "защита от любого некорректного/злонамеренного ввода",
а "всё работает корректно при нормальном, добросовестном использовании" (happy path).
Валидация и edge cases — отдельный этап, после happy path и после того, как появится
реальный опыт использования (свой + отзыв друга-тренера).

---

## До готового happy path (перед Capacitor/БД)

### Управление данными (CRUD, которого пока не хватает)

- [ ] Удаление упражнения — из каталога (страница Exercises) и/или из текущей тренировки
- [ ] Что делать при удалении последнего сета в упражнении — убирать всю карточку упражнения из тренировки, или оставлять пустую карточку?
- [ ] Рассмотреть свайп-корзину на всей карточке упражнения (не только на отдельных сетах) — для удаления упражнения целиком из тренировки

### Стилизация / полировка

- [ ] `IonModal` (выбор упражнения, новое упражнение) — доделать центрирование `.modal-content`, стили заголовка
- [ ] Toggle "Uses weight" — поправить layout, чтобы текст не переносился на 2 строки
- [ ] Проверить `text-align: end` в `.number-reps` (визуальный нюанс с прижатием цифр, признано некритичным)

### Календарь

- [ ] Синхронизация выбранного дня с реальными workouts/датами (по-хорошему зависит от БД, но можно сделать упрощённую версию на моках)

### БД

- [ ] Подключить `@capacitor-community/sqlite`, переписать моковые функции (`getCategories`, `getExercisesWithLastWeight`, `getWorkoutExercises`) на реальные SQL-запросы
- [ ] `workoutId: 0` — временная заглушка при создании нового сета/упражнения без привязки к реальной тренировке; заменить на реальный id "сегодняшней" тренировки
- [ ] Схема `Exercise`: добавить `usesWeight: boolean`, `defaultWeight?: number`

### Тестирование

- [ ] Дать другу-тренеру попробовать приложение для обратной связи по UX/функционалу, когда будет рабочая версия

---

## Валидация и edge cases (после happy path, по мере реальной необходимости)

### Форма создания упражнения

- [ ] Валидация: пустое имя, не выбрана категория — показать ошибку вместо тихого игнора
- [ ] Проверка на дубликат имени упражнения (в той же категории?) при создании

### Валидация сетов

- [ ] Проверка на `reps === 0` при добавлении нового сета через "+ add set" — не давать плодить пустые сеты подряд, подсвечивать красным последний незаполненный, пока не поправят
- [ ] Продумать, что считается "исключением" при снятии подсветки: клик на поле (снимать сразу) vs клик на кнопку сохранения с всё ещё нулевым значением (снимать / не давать сохранить?)
- [ ] Различать "поле не заполнено" от "реальный 0" — сейчас `usedWeight: 0` неотличим от "юзер не ввёл значение". Решение: `usedWeight: number | null`
- [ ] Учесть body-weight упражнения (подтягивания, отжимания, планка) — им не нужен вес, валидация по весу не должна их блокировать (частично решено через `usesWeight`)

### Удаление упражнения — soft delete

- [ ] Hard delete теряет историю (старые sets ссылаются на удалённый exerciseId) — рассмотреть soft delete через `deletedAt`/`isDeleted` вместо физического удаления
- [ ] Если soft delete — фильтровать удалённые из списков выбора (`.filter(e => !e.deletedAt)`), но оставлять видимыми в истории
- [ ] Продумать (не обязательно реализовывать): что если создать упражнение с тем же именем после удаления старого — это новая сущность с новым id, история не продолжается. Ок для первой версии

### Добавление упражнения (модалка выбора для тренировки)

- [ ] Решить: разрешать ли добавлять одно и то же упражнение дважды в одну тренировку (суперсеты/повторы) или нет
  - Сейчас: нет — фильтруются уже добавленные упражнения из списка выбора (через `workoutExercises`)
  - Если понадобится разрешить — нужен отдельный уникальный id для записи "упражнение в тренировке", не переиспользовать `exercise.id` как key
- [ ] Поисковая строка в модалке выбора упражнения — пока избыточно при малом количестве упражнений, вернуться если список вырастет

### Сохранение / БД

- [ ] При сохранении тренировки в БД — не сохранять упражнения, у которых все сеты остались с `reps === 0` (пустые/незаполненные)

еще:

- в `Exercises` может свайп для категорий
- при добавлении и переходе на `Workouts` не отображаются изначально упражнения
- категории в add exercise не крутятся
